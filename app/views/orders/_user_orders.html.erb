<div class="well">
  <div class="row">
    <div class="col-xs-4"><h3 style="font-weight: bold">Order id</h3></div>
    <div class="col-xs-4 text-center"><h3 style="font-weight: bold">Subtotal</h3></div>
    <div class="col-xs-4 text-center"><h3 style="font-weight: bold">Order status</h3></div>
  </div>
</div>
<% @orders.each do |order| %>
    <% if order.user_id==current_user.id %>
        <%= render "order_row", order: order %>
    <% end %>
<% end %>
